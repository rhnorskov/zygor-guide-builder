local ZygorGuidesViewer = ZygorGuidesViewer

if not ZygorGuidesViewer then
    print("{{title}}: ZygorGuidesViewer not found! Make sure it's installed and enabled.")
    return
end

print("{{title}}: Core loaded. Guide files will load next...")
